import { StyleSheet } from "react-native";
export const bgStyles = StyleSheet.create({
    container: {
        backgroundColor: "#2E1233",
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
    }
})